{
  "id": "code-29",
  "title": "Framework integration guide",
  "category": "coding",
  "goal": "Provide beginner-friendly, step-by-step instructions to integrate a framework into an existing project.",
  "prompt": "You are a developer. Provide step-by-step instructions to integrate {framework} into {project}. Make the instructions beginner friendly.",
  "variables": ["framework", "project"],
  "tags": ["framework-integration", "education", "programming", "tutorial"],
  "models_tested": ["gpt-5", "gemini-2.5", "claude-3"],
  "responses": {
    "gpt-5": "Nice — let’s get Tailwind set up in a Create React App (CRA) project step-by-step. I’ll give both `npm` and `yarn` commands and a tiny example you can paste in to verify it’s working.\n\n---\n\n# 1) Create a new CRA project (if you don’t already have one)\n\nUsing npm:\n\n```bash\nnpx create-react-app my-app\ncd my-app\n```\n\nOr using yarn:\n\n```bash\nyarn create react-app my-app\ncd my-app\n```\n\n---\n\n# 2) Install Tailwind and required PostCSS packages\n\nUsing npm:\n\n```bash\nnpm install -D tailwindcss postcss autoprefixer\n```\n\nUsing yarn:\n\n```bash\nyarn add -D tailwindcss postcss autoprefixer\n```\n\n---\n\n# 3) Create Tailwind + PostCSS config files\n\nRun the init command (creates `tailwind.config.js` and `postcss.config.js`):\n\n```bash\nnpx tailwindcss init -p\n```\n\nYou should now have:\n\n- `tailwind.config.js`\n- `postcss.config.js`\n\n---\n\n# 4) Configure Tailwind to scan your React files\n\nOpen `tailwind.config.js` and set the `content` (so Tailwind can remove unused CSS in production). Replace the default `content` setting with:\n\n```js\nmodule.exports = {\n  content: [\n    \"./src/**/*.{js,jsx,ts,tsx}\"\n  ],\n  theme: {\n    extend: {},\n  },\n  plugins: [],\n}\n```\n\n---\n\n# 5) Add Tailwind’s directives to your CSS\n\nOpen `src/index.css` (or create it and import in `src/index.js` if different). Replace its contents with:\n\n```css\n@tailwind base;\n@tailwind components;\n@tailwind utilities;\n\n/* Your custom styles can go below */\n```\n\nCRA already imports `src/index.css` from `src/index.js` by default, so normally you don’t need to edit imports.\n\n---\n\n# 6) Start the dev server and test\n\nStart using:\n\n```bash\nnpm start\n# or\nyarn start\n```\n\nOpen `src/App.js` and update the JSX to try few Tailwind classes, for example:\n\n```jsx\nfunction App() {\n  return (\n    <div className=\"min-h-screen flex items-center justify-center bg-gray-100\">\n      <div className=\"p-8 bg-white rounded-xl shadow-md\">\n        <h1 className=\"text-2xl font-bold mb-4\">Tailwind + CRA ✅</h1>\n        <button className=\"px-4 py-2 bg-indigo-600 text-white rounded hover:bg-indigo-500\">\n          Click me\n        </button>\n      </div>\n    </div>\n  );\n}\n\nexport default App;\n```\n\nIf the styles render, Tailwind is working.\n\n---\n\n# 7) Optional: customize theme, add plugins\n\nTo add custom colors, fonts, spacing etc., edit `theme.extend` inside `tailwind.config.js`, for example:\n\n```js\nmodule.exports = {\n  content: [\"./src/**/*.{js,jsx,ts,tsx}\"],\n  theme: {\n    extend: {\n      colors: { brand: \"#1E40AF\" }\n    },\n  },\n  plugins: [],\n}\n```\n\nThen use `className=\"bg-brand\"` in your components.\n\n---\n\n# 8) Production build notes\n\nTailwind’s purge (now `content`) will remove unused CSS automatically when you run a production build (`npm run build` / `yarn build`) as long as `tailwind.config.js` `content` paths correctly point to your source files.\n\n---\n\n# Troubleshooting checklist\n\n- If Tailwind classes do not appear, restart the dev server after changing config files.\n- Ensure classes are applied to files with extensions included in `content` (`.js`, `.jsx`, `.ts`, `.tsx`).\n- If you used a CSS file other than `src/index.css`, confirm it’s actually imported by `src/index.js`.\n- If you used older CRA versions (pre v5) you might need tools like `craco` — but for modern CRA (v5+) the steps above work without ejecting.\n\n---\n\nIf you want, I can:\n\n- show a minimal repo file tree you can copy,\n- give a ready-to-paste `tailwind.config.js` and `postcss.config.js`,\n- or adapt steps for TypeScript or an existing project. Which one next?",
    "gemini-2.5": "Hello! I can certainly guide you through integrating **Tailwind CSS** into a **Create React App (CRA)** project. This method is beginner-friendly and uses the approach recommended for CRA projects.\n\n---\n\n## 🛠️ Step-by-Step Integration\n\n### Prerequisites\n\nBefore you start, make sure you have:\n\n1. **Node.js** installed on your computer.\n2. A new or existing React project created using `create-react-app`.\n\nIf you need to create a new project, open your terminal and run:\n\n```bash\nnpx create-react-app my-tailwind-app\ncd my-tailwind-app\n```\n\n---\n\n### Step 1: Install Tailwind CSS and its Dependencies\n\nYou need to install Tailwind CSS, along with **PostCSS** and **Autoprefixer**, as development dependencies. PostCSS is a tool that transforms CSS with JavaScript plugins, and Autoprefixer adds vendor prefixes for better browser compatibility.\n\n```bash\nnpm install -D tailwindcss postcss autoprefixer\n```\n\n---\n\n### Step 2: Create Configuration Files\n\nGenerate Tailwind and PostCSS configuration files:\n\n```bash\nnpx tailwindcss init -p\n```\n\nThis creates:\n- `tailwind.config.js`\n- `postcss.config.js`\n\n---\n\n### Step 3: Configure Template Paths in `tailwind.config.js`\n\nOpen the new `tailwind.config.js` file and update the `content` property:\n\n```js\nmodule.exports = {\n  content: [\n    \"./src/**/*.{js,jsx,ts,tsx}\",\n  ],\n  theme: {\n    extend: {},\n  },\n  plugins: [],\n}\n```\n\n---\n\n### Step 4: Add Tailwind Directives to CSS\n\nOpen `src/index.css` and replace everything with:\n\n```css\n@tailwind base;\n@tailwind components;\n@tailwind utilities;\n```\n\n---\n\n### Step 5: Start the App\n\nStart your development server:\n\n```bash\nnpm start\n```\n\n---\n\n### Step 6: Test Tailwind CSS\n\nOpen `src/App.js` and try:\n\n```jsx\nfunction App() {\n  return (\n    <div className=\"flex justify-center items-center min-h-screen bg-gray-100\">\n      <h1 className=\"text-4xl font-bold text-indigo-600 p-4 border-b-4 border-indigo-600\">\n        Hello Tailwind in React!\n      </h1>\n    </div>\n  );\n}\n\nexport default App;\n```\n\nIf the text appears styled and centered, you’re all set!",
    "claude-3": "# Integrating Tailwind CSS into Create React App\n\n## Step 1: Create Your React App\n\n```bash\nnpx create-react-app my-app\ncd my-app\n```\n\n## Step 2: Install Tailwind CSS\n\n```bash\nnpm install -D tailwindcss postcss autoprefixer\n```\n\n## Step 3: Generate Configuration Files\n\n```bash\nnpx tailwindcss init -p\n```\n\nCreates `tailwind.config.js` and `postcss.config.js`.\n\n## Step 4: Configure Template Paths\n\n```js\nmodule.exports = {\n  content: [\n    \"./src/**/*.{js,jsx,ts,tsx}\",\n  ],\n  theme: {\n    extend: {},\n  },\n  plugins: [],\n}\n```\n\n## Step 5: Add Tailwind Directives\n\nReplace `src/index.css` content:\n\n```css\n@tailwind base;\n@tailwind components;\n@tailwind utilities;\n```\n\n## Step 6: Use Tailwind in a Component\n\n```jsx\nfunction App() {\n  return (\n    <div className=\"flex items-center justify-center h-screen bg-blue-500\">\n      <h1 className=\"text-4xl font-bold text-white\">Hello Tailwind!</h1>\n    </div>\n  );\n}\n\nexport default App;\n```\n\n## Step 7: Run the App\n\n```bash\nnpm start\n```\n\nTailwind should now be working!"
  },
  "last_updated": "2025-11-03"
}
