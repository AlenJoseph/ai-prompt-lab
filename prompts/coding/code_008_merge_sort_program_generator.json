{
    "id": "code-003",
    "title": "Merge Sort Program Generator",
    "category": "coding",
    "goal": "Generate and explain a Merge Sort algorithm in the specified language",
    "prompt": "Write a {language} program that sorts the numbers using a merge sort algorithm. It should include a complete program, step-by-step explanation of how code works, example input and output for how code works, and time and space complexity of the algorithm.",
    "variables": ["language"],
    "tags": ["algorithm", "sorting", "merge-sort", "education"],
    "models_tested": ["gpt-5", "gemini-2.5", "claude-3"],
    "responses": {
        "gpt-5": "# Merge Sort in Python — complete program, explanation, example, and complexity\n\nBelow is a complete, runnable Python program that implements **merge sort**, followed by a step-by-step explanation of how it works, a worked example (input → output), and the time & space complexity.\n\n```python\n# merge_sort.py\nfrom typing import List\n\ndef merge(left: List[int], right: List[int]) -> List[int]:\n    \"\"\" Merge two sorted lists `left` and `right` and return a single sorted list. \"\"\"\n    i = j = 0\n    merged = []\n\n    # Compare elements from left and right and append the smaller one\n    while i < len(left) and j < len(right):\n        if left[i] <= right[j]:\n            merged.append(left[i])\n            i += 1\n        else:\n            merged.append(right[j])\n            j += 1\n\n    # Append any remaining elements\n    if i < len(left):\n        merged.extend(left[i:])\n    if j < len(right):\n        merged.extend(right[j:])\n\n    return merged\n\ndef merge_sort(arr: List[int]) -> List[int]:\n    \"\"\" Perform merge sort on list `arr` and return a new sorted list. \"\"\"\n    n = len(arr)\n    if n <= 1:\n        return arr[:]\n    mid = n // 2\n    left_sorted = merge_sort(arr[:mid])\n    right_sorted = merge_sort(arr[mid:])\n    return merge(left_sorted, right_sorted)\n\ndef main():\n    s = input(\"Enter integers separated by spaces (or press Enter to use an example): \").strip()\n    if s == \"\":\n        arr = [38, 27, 43, 3, 9, 82, 10]\n        print(\"No input given — using example list:\", arr)\n    else:\n        try:\n            arr = [int(x) for x in s.split()]\n        except ValueError:\n            print(\"Invalid input. Please enter integers separated by spaces.\")\n            return\n    sorted_arr = merge_sort(arr)\n    print(\"Sorted list:\", sorted_arr)\n\nif __name__ == \"__main__\":\n    main()\n```\n\n---\n\n### 🧠 How the code works\n1. **Divide and conquer** — the list is split recursively until sublists of 1 element remain.\n2. **Merge** — two sorted sublists are merged by comparing front elements and appending the smaller.\n3. **Repeat** — merged lists build up until the full list is sorted.\n\n### 🧩 Example\n```\nInput: [38, 27, 43, 3, 9, 82, 10]\nOutput: [3, 9, 10, 27, 38, 43, 82]\n```\n\n### ⏱️ Complexity\n- Time: **O(n log n)** (best, average, and worst)\n- Space: **O(n)** (due to extra merged lists)\n",
        "gemini-2.5": "Merge Sort is a divide-and-conquer algorithm that divides an array into halves, recursively sorts them, and merges them back. Below is a detailed explanation and Python implementation.\n\n```python\ndef merge_sort(arr):\n    if len(arr) <= 1:\n        return arr\n    mid = len(arr) // 2\n    left_half = merge_sort(arr[:mid])\n    right_half = merge_sort(arr[mid:])\n    return merge(left_half, right_half)\n\ndef merge(left, right):\n    merged = []\n    i = j = 0\n    while i < len(left) and j < len(right):\n        if left[i] <= right[j]:\n            merged.append(left[i])\n            i += 1\n        else:\n            merged.append(right[j])\n            j += 1\n    merged.extend(left[i:])\n    merged.extend(right[j:])\n    return merged\n\nif __name__ == \"__main__\":\n    arr = [38, 27, 43, 3, 9, 82, 10]\n    print(\"Original:\", arr)\n    sorted_arr = merge_sort(arr)\n    print(\"Sorted:\", sorted_arr)\n```\n\n### Step-by-Step\n1. **Divide:** Split list into halves recursively until lists of size 1.\n2. **Conquer:** Sort left and right halves.\n3. **Combine:** Merge sorted halves while maintaining order.\n\n### Example\nInput → `[5, 2, 9, 1, 5, 6]`\nOutput → `[1, 2, 5, 5, 6, 9]`\n\n### Complexity\n- Time: O(n log n)\n- Space: O(n)\n",
        "claude-3": "def merge_sort(arr):\n    \"\"\"Sorts an array using merge sort.\"\"\"\n    if len(arr) <= 1:\n        return arr\n    mid = len(arr) // 2\n    left = merge_sort(arr[:mid])\n    right = merge_sort(arr[mid:])\n    return merge(left, right)\n\ndef merge(left, right):\n    result = []\n    i = j = 0\n    while i < len(left) and j < len(right):\n        if left[i] <= right[j]:\n            result.append(left[i])\n            i += 1\n        else:\n            result.append(right[j])\n            j += 1\n    result.extend(left[i:])\n    result.extend(right[j:])\n    return result\n\nif __name__ == \"__main__\":\n    data = [64, 34, 25, 12, 22, 11, 90]\n    print(\"Original:\", data)\n    print(\"Sorted:\", merge_sort(data))\n\n### Explanation\n- **Divide:** Split list recursively into two halves.\n- **Merge:** Combine sorted halves while comparing smallest elements.\n- **Result:** Sorted array.\n\n### Example Output\nOriginal: [64, 34, 25, 12, 22, 11, 90]\nSorted: [11, 12, 22, 25, 34, 64, 90]\n\n### Complexity\n- Time: O(n log n)\n- Space: O(n)\n"
    },
    "last_updated": "2025-10-31"
}
